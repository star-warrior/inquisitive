---
phase: 01-redis-and-ip-based-rate-limiting-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/package.json, backend/src/config/rateLimiter.ts, backend/src/app.ts]
autonomous: true
requirements: [REQ-SEC-01, REQ-SEC-02, REQ-SEC-03, REQ-SEC-04]

# Goal-backward verification (derived during planning, verified after execution)
must_haves:
  truths:
    - "General API rate limiting is enforced using Redis store."
    - "AI endpoints are protected by a strict Redis-backed 5 requests/hour limiter."
  artifacts:
    - path: "backend/src/config/rateLimiter.ts"
      provides: "Redis-backed rate limiters"
      contains: "new RedisStore"
    - path: "backend/src/app.ts"
      provides: "Express rate limit middleware registration"
      contains: "app.use(generalLimiter)"
  key_links:
    - from: "backend/src/app.ts"
      to: "backend/src/config/rateLimiter.ts"
      via: "imports generalLimiter"
      pattern: "import.*generalLimiter.*rateLimiter"
---

<objective>
Refactor Inquisitive's rate limiting mechanism to use Redis as the store, tracking client requests based on client IP addresses.

Purpose: Protect endpoints from brute-force/abuse while ensuring multi-instance backend compatibility.
Output: Refactored rate limiter config using `rate-limit-redis` and corrected main application middleware integration.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/package.json
@backend/src/config/rateLimiter.ts
@backend/src/app.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Redis dependencies in backend</name>
  <files>backend/package.json</files>
  <read_first>backend/package.json</read_first>
  <action>
    Add `redis` and `rate-limit-redis` to the dependencies in backend.
    Run npm install from the root or within the backend workspace to download and configure these packages.
  </action>
  <verify>npm run build in backend succeeds</verify>
  <acceptance_criteria>
    - backend/package.json contains "redis" in dependencies
    - backend/package.json contains "rate-limit-redis" in dependencies
  </acceptance_criteria>
  <done>Redis packages are installed and registered in dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Refactor rateLimiter.ts with RedisStore</name>
  <files>backend/src/config/rateLimiter.ts</files>
  <read_first>backend/src/config/rateLimiter.ts, backend/src/utils/logger.ts</read_first>
  <action>
    Import createClient from redis and RedisStore from rate-limit-redis.
    Create a Redis client instance, taking REDIS_URL from environment variables (default to redis://localhost:6379).
    Connect the Redis client asynchronously and log success or error using the logger.
    Refactor generalLimiter and aiLimiter to configure the RedisStore using the initialized Redis client.
    Configure standard headers and beautiful JSON error responses.
  </action>
  <verify>npm run build in backend succeeds</verify>
  <acceptance_criteria>
    - backend/src/config/rateLimiter.ts contains "import { createClient } from 'redis'" or equivalent
    - backend/src/config/rateLimiter.ts contains "import { RedisStore } from 'rate-limit-redis'" or equivalent
    - backend/src/config/rateLimiter.ts sets the store option in rateLimit to new RedisStore
  </acceptance_criteria>
  <done>rateLimiter.ts successfully uses RedisStore for rate limits</done>
</task>

<task type="auto">
  <name>Task 3: Apply generalLimiter in backend app.ts</name>
  <files>backend/src/app.ts</files>
  <read_first>backend/src/app.ts, backend/src/config/rateLimiter.ts</read_first>
  <action>
    Refactor app.ts to import generalLimiter from config/rateLimiter.js.
    Replace the broken global app.use(rateLimit) middleware with app.use(generalLimiter).
    Ensure the trust proxy configuration is correct.
  </action>
  <verify>npm run build in backend succeeds</verify>
  <acceptance_criteria>
    - backend/src/app.ts contains "import { generalLimiter } from './config/rateLimiter.js'"
    - backend/src/app.ts contains "app.use(generalLimiter)"
    - backend/src/app.ts does NOT contain "app.use(rateLimit)"
  </acceptance_criteria>
  <done>app.ts applies generalLimiter correctly</done>
</task>

</tasks>

<verification>
- [ ] npm run build in backend succeeds
- [ ] Server starts successfully with Redis logger confirmations
</verification>

<success_criteria>
- Redis rate limiters successfully refactored and integrated
- Standard headers and IP limiters are active
</success_criteria>

<output>
After completion, create `.planning/phases/01-redis-and-ip-based-rate-limiting-refactor/01-01-SUMMARY.md`
</output>
