---
phase: 04-loader-and-notebook-limits
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/src/services/notebook.service.ts, backend/src/routes/noteBook.route.ts, frontend/src/pages/LandingPage.tsx, frontend/src/features/LandingPage/components/Loader.tsx]
autonomous: true
requirements: [REQ-LNL-01, REQ-LNL-02, REQ-LNL-03, REQ-LNL-04]

must_haves:
  truths:
    - "LandingPage polls backend /health endpoint on mount."
    - "LandingPage preloads the hero image using JavaScript Image constructor."
    - "LandingPage loader screen is shown until healthcheck returns 200 OK and hero image is loaded."
    - "Backend countUserNotebooks counts notebooks by deviceId (useruuid)."
    - "Backend /create route returns a 403 response with a notification if notebook count >= 5."
  artifacts:
    - path: "backend/src/services/notebook.service.ts"
      provides: "Backend service for notebook count checking"
      contains: "countUserNotebooks"
    - path: "backend/src/routes/noteBook.route.ts"
      provides: "Notebook route checking limits before creation"
      contains: "countUserNotebooks"
    - path: "frontend/src/pages/LandingPage.tsx"
      provides: "Healthcheck and image preloading landing page"
      contains: "isBackendReady"
    - path: "frontend/src/features/LandingPage/components/Loader.tsx"
      provides: "Isolated Landing Page Loader screen UI"
      contains: "Loader"
---

<objective>
Implement healthcheck polling and hero image preloading on the Landing Page, and enforce notebook limits on the backend create route.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/src/app.ts
@backend/src/routes/noteBook.route.ts
@frontend/src/pages/LandingPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create countUserNotebooks Backend Service</name>
  <files>backend/src/services/notebook.service.ts</files>
  <read_first>backend/src/models/schema.ts, backend/src/config/db.ts</read_first>
  <action>
    Create `backend/src/services/notebook.service.ts` to export `countUserNotebooks`:
    ```typescript
    import { db } from "../config/db.js";
    import { noteBook } from "../models/schema.js";
    import { eq, sql } from "drizzle-orm";

    export const countUserNotebooks = async (useruuid: string): Promise<number> => {
      const result = await db
        .select({ count: sql<number>`count(*)` })
        .from(noteBook)
        .where(eq(noteBook.deviceId, useruuid));
      return Number(result[0]?.count ?? 0);
    };
    ```
  </action>
  <verify>Check file existence and content</verify>
  <acceptance_criteria>
    - backend/src/services/notebook.service.ts exists and compiles without errors.
    - Exports `countUserNotebooks` which returns a number representing the count of notebooks created by the user.
  </acceptance_criteria>
  <done>notebook.service.ts is created</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Limit Check in /create Notebook Route</name>
  <files>backend/src/routes/noteBook.route.ts</files>
  <read_first>backend/src/routes/noteBook.route.ts, backend/src/services/notebook.service.ts</read_first>
  <action>
    Modify `backend/src/routes/noteBook.route.ts`:
    1. Import `countUserNotebooks` from `../services/notebook.service.js`.
    2. Inside `router.post("/create", ...)`:
       - Before calling `createNoteBook(req.body, validUseruuid.data)`, check the current count of notebooks:
         ```typescript
         const count = await countUserNotebooks(validUseruuid.data);
         if (count >= 5) {
           res.status(403).json({
             success: false,
             errorType: "FREE_TIER_LIMIT",
             message: "Free tier only allows 5 notebooks. Upgrade to create more.",
           });
           return;
         }
         ```
  </action>
  <verify>Check code changes in route</verify>
  <acceptance_criteria>
    - noteBook.route.ts imports `countUserNotebooks`.
    - Returns 403 if count is 5 or more, with appropriate JSON body.
  </acceptance_criteria>
  <done>/create route limits notebooks to 5</done>
</task>

<task type="auto">
  <name>Task 3: Implement Landing Page Healthcheck Loader and Image Preloader</name>
  <files>frontend/src/pages/LandingPage.tsx</files>
  <read_first>frontend/src/pages/LandingPage.tsx, frontend/src/features/dashboard/components/LoadingScreen.tsx, frontend/src/features/LandingPage/components/Hero.tsx</read_first>
  <action>
    Modify `frontend/src/pages/LandingPage.tsx`:
    1. Import `useState`, `useEffect` from `react`.
    2. Import `axios` from `axios`.
    3. Import `heroBg` from `../assets/images/hero-page.png`.
    4. Implement states:
       - `isBackendReady: boolean` (default false)
       - `isHeroImageLoaded: boolean` (default false)
    5. In `useEffect`:
       - Poll the `/health` endpoint every 1.5 seconds. The base URL can be read from `import.meta.env.VITE_API_URL` or fallback to `http://localhost:3000`.
       - Create an `Image` object, set its source to `heroBg`, and set `isHeroImageLoaded` to true in `onload` and `onerror`.
    6. Render a beautiful full-screen loading screen overlay when `!isBackendReady || !isHeroImageLoaded`:
       - Frame-motion or nice CSS animations.
       - A centered glassmorphic card with a modern spinner, branding, and text: "Starting up services..."
    7. Once both are ready, render the landing page components.
  </action>
  <verify>Check that LandingPage.tsx mounts components when ready</verify>
  <acceptance_criteria>
    - LandingPage.tsx includes polling logic and hero image preloading.
    - Displays loading screen overlay if health check is pending or image is not cached.
  </acceptance_criteria>
  <done>Loader and preloading are implemented on LandingPage</done>
</task>

</tasks>

<verification>
- [ ] Backend route limits notebooks to 5.
- [ ] Landing page shows loader until health check is 200 OK and hero image is loaded.
</verification>

<success_criteria>
- Backend `/create` route rejects requests if notebook count >= 5.
- Frontend landing page loader is smooth and preloads hero image.
</success_criteria>

<output>
Create `.planning/phases/04-loader-and-notebook-limits/04-01-SUMMARY.md` on completion.
</output>
